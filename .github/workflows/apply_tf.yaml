name: matrix
on: push

jobs:
  Node-Versions:
    runs-on: ubuntu-latest
    steps:
    - name: Node Version
      runs: node -v
    - uses: actions/setup-node@v2
      with:
        node-version: 8
    - name: After setup-node node Version
      run: node -v